# 🧩 🔐 Автоматическая инициализация администратора

## Описание

Система автоматически создает первого администратора при первом запуске приложения, если администратор еще не существует в базе данных.

## Конфигурация

Параметры администратора настраиваются в `application.yml` или через переменные окружения:

```yaml
app:
  admin:
    username: ${ADMIN_USERNAME:admin}                    # Имя пользователя (по умолчанию: admin)
    email: ${ADMIN_EMAIL:admin@demo.com}                 # Email (по умолчанию: admin@demo.com)
    password: ${ADMIN_PASSWORD:admin}                     # Пароль (по умолчанию: admin)
    fullName: ${ADMIN_FULL_NAME:Администратор}           # Полное имя (по умолчанию: Администратор)
    passwordChangeRequired: ${ADMIN_PASSWORD_CHANGE_REQUIRED:true}  # Требовать смену пароля (по умолчанию: true)
```

### Использование переменных окружения

Для продакшена рекомендуется использовать переменные окружения:

```bash
export ADMIN_USERNAME=admin
export ADMIN_EMAIL=admin@example.com
export ADMIN_PASSWORD=SecurePassword123!
export ADMIN_FULL_NAME="Главный Администратор"
export ADMIN_PASSWORD_CHANGE_REQUIRED=true
```

## Как это работает

1. **При первом запуске приложения:**
   - Система проверяет, существует ли администратор в базе данных
   - Если администратора нет, создается новый администратор с параметрами из конфигурации
   - Пароль хешируется с помощью BCrypt перед сохранением
   - Если `passwordChangeRequired=true`, администратор будет вынужден сменить пароль при первом входе

2. **При последующих запусках:**
   - Если администратор уже существует, новый администратор не создается
   - Существующие данные не изменяются

## Смена пароля

### Endpoint для смены пароля

**POST** `/api/auth/change-password`

**Требования:**
- Пользователь должен быть аутентифицирован (передан токен в заголовке Authorization)
- Для первого входа (когда `passwordChangeRequired=true`) можно не указывать `currentPassword`

**Тело запроса:**
```json
{
  "currentPassword": "старый_пароль",  // Опционально для первого входа
  "newPassword": "новый_пароль",
  "confirmPassword": "новый_пароль"
}
```

**Успешный ответ (200 OK):**
```json
{
  "message": "Password changed successfully",
  "success": true
}
```

**Ошибка (400 Bad Request):**
```json
{
  "errors": {
    "newPassword": ["Password must be at least 6 characters"],
    "confirmPassword": ["Passwords do not match"],
    "currentPassword": ["Current password is incorrect"]
  }
}
```

### Пример использования через curl

```bash
# Смена пароля (требуется аутентификация)
curl -X POST http://localhost:8080/api/auth/change-password \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "currentPassword": "admin",
    "newPassword": "NewSecurePassword123!",
    "confirmPassword": "NewSecurePassword123!"
  }'
```

## Логирование

При создании администратора в логах будет выведено:

```
═══════════════════════════════════════════════════════════════
⚠️  ВНИМАНИЕ: Администратор создан с временным паролем!
⚠️  Username: admin
⚠️  Password: admin
⚠️  Необходимо сменить пароль при первом входе!
═══════════════════════════════════════════════════════════════
```

## Безопасность

### Рекомендации для продакшена:

1. **Используйте переменные окружения** для паролей вместо хардкода в `application.yml`
2. **Установите `passwordChangeRequired=true`** чтобы принудительно сменить пароль при первом входе
3. **Используйте сильные пароли** (минимум 12 символов, смешанный регистр, цифры, спецсимволы)
4. **Не коммитьте** `application.yml` с реальными паролями в Git
5. **Используйте секреты** в вашей системе развертывания (Kubernetes Secrets, Docker Secrets, etc.)

### Пример для Docker:

```yaml
# docker-compose.yml
services:
  backend:
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_EMAIL=admin@example.com
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}  # Из .env файла
      - ADMIN_PASSWORD_CHANGE_REQUIRED=true
```

## Проверка статуса

Для проверки, создан ли администратор, используйте:

**GET** `/api/auth/check-users`

Ответ:
```json
{
  "totalUsers": 1,
  "message": "Admin user is created automatically on first startup",
  "config": {
    "adminUsername": "admin (configurable via app.admin.username)",
    "adminEmail": "admin@demo.com (configurable via app.admin.email)",
    "passwordChangeRequired": "true (configurable via app.admin.passwordChangeRequired)"
  }
}
```

## Преимущества этого подхода

✅ **Безопасность:** Нет публичного endpoint для создания администратора  
✅ **Контроль:** Процесс контролируется через конфигурацию  
✅ **Автоматизация:** Администратор создается автоматически при первом запуске  
✅ **Гибкость:** Параметры настраиваются через конфигурацию или переменные окружения  
✅ **Безопасность паролей:** Принудительная смена пароля при первом входе  
